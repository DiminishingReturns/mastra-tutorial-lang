import { generatedCalibrationSet } from '../problem-bank/examples/normalDistribution/generatedCalibrationSet';

//console.log(JSON.stringify(generatedCalibrationSet, null, 2));

// Start-end of course contexts: [12 - 61]

const calibrationExamples = generatedCalibrationSet[59]; //change index: 0, 1, 2, or 3

console.log(JSON.stringify(calibrationExamples, null, 2));
